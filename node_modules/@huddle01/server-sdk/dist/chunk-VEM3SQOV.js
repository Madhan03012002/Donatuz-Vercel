import { SDK_VERSION, INFRA_URL } from './chunk-2ZBCNKEG.js';
import { hc } from 'hono/client';

var getInfraClient = (data) => {
  const obj = {
    headers: {
      "x-api-key": data.apiKey,
      "x-sdk-version": SDK_VERSION
    }
  };
  const api = hc(INFRA_URL, obj);
  return api;
};

export { getInfraClient };
